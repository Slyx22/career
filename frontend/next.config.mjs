/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Netlify's Next.js runtime (@netlify/plugin-nextjs) handles SSR/ISR
  // automatically - no special output mode required here. See
  // netlify.toml and docs/NETLIFY.md for deployment configuration.
};

export default nextConfig;
